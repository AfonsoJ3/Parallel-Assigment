#include <stdio.h>
#include <stdlib.h>
#include "mm-student.h"
#include <time.h>

void rd_number(double* array, int n);

//Main function
int main(int argc, char **argv)
{
	int n = 10;

	//first array and second array
	double* f_matrix = (double*)malloc(n*sizeof(double));
	double* s_matrix = (double*)malloc(n*sizeof(double));
	
	if(f_matrix == NULL || s_matrix == NULL)
	{
		printf("Memory allocation failed!");
		return 0;
	}

// generates random numbers

	assignMatrix(f_matrix, n);
	assignMatrix(s_matrix, n);

	free(f_matrix);
	free(s_matrix);
	return 0;
}

void assignMatrix(double* mat, int n)
{
	//Stores the random numbers.
	double* rdNum = (double*) malloc((double *) n*sizeof(double));
	//generates random numbers between -100 and 100
	rd_number(rdNum, n);

	for (int i = 0; i < n; i++)
	{	
		for (int j = 0; j < n; j++)
		{
			mat[i * n + rdNUm[j]] = (i + 1) * (j + 1);
		}
	}

	free(rdNum);
}

void 
